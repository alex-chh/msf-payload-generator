# MSFVenom Payload Generator - Dependencies
# Python tool designed for automating Metasploit payload generation

# Core
python>=3.8.0

# Standard library (no extra install)
# subprocess, os, sys, json, typing

# Dev and test (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.900

# Remote execution (system-level)
# openssh-client (SSH/SCP support)
# metasploit-framework (msfvenom)

# Optional features
# paramiko>=2.0.0  # advanced SSH support (future)
# scp>=0.14.0      # SCP protocol support (future)

# Docs
sphinx>=4.0.0
sphinx-rtd-theme>=1.0.0

# Packaging
setuptools>=60.0.0
twine>=4.0.0

# Note: this tool primarily relies on system utilities (msfvenom, ssh, scp)
# Python code uses only the standard library to maximize compatibility
